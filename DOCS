
# labels

	labelmarker <filename> <rule> <alpha>
	labelfont <font>		(default TimesRoman)
	labelsize 12			(default 12 pikseliä)
	labelstroke <color> <rule>	(default Black,Copy)
	labelfill <color> <rule>	(default Black,Copy)
	labelalign <alignment>		(default Center)
	labelformat <format>		(default none, empty means no output)
	labelangle <angle>		(default is 0)
	labeloffset <dx> <dy>		(default 0,0)
	label <lon> <lat>
	labelxy <lon> <lat> <x> <y>
	labels <dx> <dy>
	labelfile <filename>		(label lon lat dataa, kommentit ok)
	labelcaption <name> <dx> <dy> <align>

	clear labels			(only latlon locations are cleared)

#filtering
	timestep <integer>	aika-askeleen pituus minuutteina
	timesteprounding <1|0>	pyöristetäänkö timestep monikertaan?
	timeinterval <integer>	aito intervalli, yleensä yo, tai -1 minuutti
	timesteps <integer>	aika-askelten lukumäärä
	timestepskip <integer>	alusta skipattava lukumäärä
	filter <none|linear|mean|sum|min|max>

	hilimit <limit>
	datareplace <sourcevalue> <targetvalue>
	datalolimit <limit>
	datahilimit <limit>

# smoothing data

	contourinterpolation <Nearest|Linear>
	smoother <None|Neighbourhood|PseudoGaussian>
	smootherradius <float>
	smootherfactor <int>

# timestamp
	timestampimagexy <x> <y>
	timestampimage <none|obs|for|forobs>

	
Rendering values:

	Toistaiseksi tunnistetaan vain fontit /var/www/share/hershey/*,
	jotka eivöt ole kovin kummoisia laadultaan.

	Voi kättää pelkkää markkeria, pelkkiä labeleita, tai kumpiakin.
	Markkerilla ei ole offsettia, labelilla on.

	labels toimii vain markkereille, ei hila-arvoille (toistaiseksi)


These affect only the parameter:


